import 'package:flutter/material.dart';

class ProfessionalData extends StatelessWidget {
  const ProfessionalData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
